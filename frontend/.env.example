# Frontend env (Vike + Vite). `PUBLIC_*` keys are exposed to the browser bundle.
PUBLIC_API_URL=http://localhost:8000/api
PUBLIC_ADMIN_URL=http://localhost:8000/admin
PUBLIC_YANDEX_MAPS_API_KEY=
# SSR-side URL used by the in-container fetch (server-only).
API_URL_SSR=http://backend:8000/api
